#include<stdio.h>
int main()
{
    int a,res;
    scanf("%d",&a);
    if (a > 0)
    {
        printf("%d is a positve number.", a);
    }
    else
    {
        printf("%d is a negative number.", a);
    }

    return 0;
}
